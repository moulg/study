#remark
--数据表加载
require "data.shop_item_config"
require "data.item_config"
require "data.message_config"
require "data.item_attribute_config"
require "data.player_head_src"
require "data.game_config" --游戏配置
require "data.http_address_cfg" --超连接配置
require "data.game_classify_cfg" --游戏分类表
require "data.scroll_text_pos_cfg" --跑马灯位置配置
require "data.item_src_config" --道具资源配置
require "data.item_effect_config" --道具特效配置
require "data.talk_font_config"
require "data.sliderRes_config"
require "data.lobby_config"
require "data.string_src_cfg"
require "data.scroll_panel_config"
require "data.text_color_config"
require "data.horm_color_config"--喇叭颜色配置表
require "data.spine_cfg"                          --spine
require "data.linkLabel_color_cfg"                          --超文本配置表
require "data.bank_bangding_config"


-- 愤怒、悲伤、受伤、杀害，你背负着痛苦之火。

-- 痛苦吗？和你内心的野兽战斗，然后成为人吧。

-- 确实每个人的心中都有一只野兽，但是却和一般的野兽不同——心。人有心，人越偏向野兽越痛苦，你刚才说你很痛苦，那正是你身为人的证明。

-- 谁都会有偏离人道暴露出野兽本性的时候，所以才要和心中的野兽进行战斗。不要恨别人，要恨就恨你自己，恨你自己心中的那头野兽。
